import { CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function SettingsHeader() {
  return (
    <CardHeader>
      <CardTitle>Based Math Game</CardTitle>
      <CardDescription>
        The Base Math Game is a practice tool for converting between different
        bases and number representations.
      </CardDescription>
    </CardHeader>
  );
}
